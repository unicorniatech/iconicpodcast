# ICONIC Podcast Environment Variables
# Copy this file to .env and fill in your values

# =============================================================================
# SUPABASE (Required for production)
# Get these from your Supabase project settings
# =============================================================================
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# =============================================================================
# GEMINI API (Server-side only - DO NOT prefix with VITE_)
# This key should only be used in serverless functions, never in frontend
# =============================================================================
GEMINI_API_KEY=your_gemini_api_key

# =============================================================================
# API Configuration
# =============================================================================
VITE_API_URL=/api

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. For Vercel/Netlify deployment:
#    - Set GEMINI_API_KEY as a secret environment variable (not public)
#    - Set VITE_* variables as regular environment variables
#
# 2. For local development:
#    - Copy this file to .env
#    - The dev server will proxy /api requests to your backend
#
# 3. Supabase setup:
#    - Run the SQL migration in supabase/migrations/001_initial_schema.sql
#    - Create an admin user and add them to the admin_users table
